import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    padding: 30px 0;
    height: calc(100% - 80px); /* Só funciona pq no global foi setado que o height e 100% */

    
`;